/**
 * Logging-related types for ChalkOps Platform
 */

/**
 * Logging levels
 */
export enum LogLevel {
  DEBUG = 'debug',
  INFO = 'info',
  WARN = 'warn',
  ERROR = 'error',
}

/**
 * Logging formats
 */
export enum LogFormat {
  JSON = 'json',
  TEXT = 'text',
} 