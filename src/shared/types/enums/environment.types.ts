/**
 * Environment-related types for ChalkOps Platform
 */

/**
 * Supported environment types
 */
export enum Environment {
  DEVELOPMENT = 'development',
  STAGING = 'staging',
  PRODUCTION = 'production',
} 